// A tool for generating multiple random sudoku problems
// Usage: ./generate-sudoku <number of questions> <difficulty>
//

#include "generator/generator.h"

#include <string>
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
    // int numOfQuestions = atoi(argv[1]);
    // int difficulty = atoi(argv[2]);
    // for (int i=0;i<numOfQuestions;i++) {
    //     std::string fileName = "sudoku-" + std::to_string(i) + "-test.csv";
    //     printf("create sudoku problem in the file: %s\n", fileName.c_str());
    //     generator::createSudoku(difficulty, fileName);
    // }

    // return 0;
}
